import "../Config.pkl"

alejandra = new Config.Step {
  category = "Nix"
  description = "Alternative Nix formatter"
  glob = "**/*.nix"
  stage = "<JOB_FILES>"
  check = "alejandra --check {{ files }}"
  fix = "alejandra {{ files }}"
}
