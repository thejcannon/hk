import "../Config.pkl"

black = new Config.Step {
  category = "Python"
  description = "Opinionated Python formatter"
  glob = List("**/*.py")
  stage = "<JOB_FILES>"
  check = "black --check {{ files }}"
  check_diff = "black --diff {{ files }}"
  fix = "black {{ files }}"
}
